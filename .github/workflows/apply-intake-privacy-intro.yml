name: Apply intake privacy intro
on:
  push:
    paths:
      - '.github/scripts/apply-intake-privacy-intro.js'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node .github/scripts/apply-intake-privacy-intro.js
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html netlify/functions/lib/whatsappConversation.js
          git diff --cached --quiet || git commit -m "Add privacy and process intro to intake"
          git push
