name: Apply request info UI fix

on:
  push:
    paths:
      - '.github/workflows/apply-request-info-ui-fix.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Sync main
        run: git pull --rebase origin main
      - name: Apply patch
        run: node .github/scripts/apply-request-info-ui-fix.js
      - name: Commit patch and cleanup
        run: |
          rm -f .github/scripts/apply-request-info-ui-fix.js .github/workflows/apply-request-info-ui-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add admin-workstation.html .github/scripts/apply-request-info-ui-fix.js .github/workflows/apply-request-info-ui-fix.yml
          git commit -m "Wire admin information requests to client delivery"
          git push origin HEAD:main
