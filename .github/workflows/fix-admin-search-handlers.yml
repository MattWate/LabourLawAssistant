name: Fix admin search handlers
on:
  push:
    paths:
      - '.github/workflows/fix-admin-search-handlers.yml'
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: git pull --rebase origin main
      - run: node .github/scripts/fix-admin-search-handlers.js
      - run: |
          rm -f .github/scripts/fix-admin-search-handlers.js .github/workflows/fix-admin-search-handlers.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add admin-workstation.html .github/scripts/fix-admin-search-handlers.js .github/workflows/fix-admin-search-handlers.yml
          git commit -m "Fix admin search event handlers"
          git push origin HEAD:main
